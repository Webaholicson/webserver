config.site {
    name        = "localhost",
    dir         = "var/www",
    error_log   = "log/error.log",
    access_log  = "log/access.log",
    email       = "user@localhost",
    index       = "index.html",
}